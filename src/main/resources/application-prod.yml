# 檔案用途：專案設定檔，說明 src/main/resources/application-prod.yml 的用途與本機開發設定。
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
