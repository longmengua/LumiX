# P13-T04 - 建立 repository / persistence access convention

## 狀態

```text
completed
```

## 目標

建立 persistence 存取邊界與 repository convention，讓後續 application layer 只能透過 repository / port 存取資料，不直接接觸 database client。

## Required context

```text
AGENTS.md
AI_AGENT.md
AI_PROGRESS.md
docs/ai/AI_CONTEXT_ROUTING.md
docs/phases/PHASE_13_IDENTITY_ACCOUNT/README.md
server/docs/backend-module-boundary.md
server/docs/error-response-boundary.md
server/docs/dto-conventions.md
server/docs/validation-conventions.md
server/docs/persistence-boundary.md
```

## Scope

```text
- 新增 persistence / repository convention 文件
- 新增 repository marker interface 或 package skeleton
- 新增 persistence exception boundary
- 新增測試，驗證 persistence boundary 不暴露 SQL / internal exception 給 API 層
- 更新 backend-module-boundary 文件
```

## Implementation rules

- 不要實作 runtime money movement。
- 不要實作 ledger posting。
- 不要實作 reservation hold / release。
- 不要實作 order matching。
- 不要實作 settlement。
- 不要實作 withdrawal signing / broadcast。
- 不要實作 deposit crediting。
- 不要新增或修改 Flyway migration。
- 不要改前端。
- 所有新增 public class / public method 必須有繁體中文維護性註解。

## Verification

```text
Run the focused persistence boundary test and the relevant Maven test compile.
```

## Risk

```text
Low-medium。主要風險是後續真正 repository 實作若直接回傳 DTO，會違反 boundary。
```

## Completion notes

```text
Implemented by: Codex
Date: 2026-07-07
Files changed:
- docs/phases/PHASE_13_IDENTITY_ACCOUNT/README.md
- docs/phases/PHASE_13_IDENTITY_ACCOUNT/tasks/P13-T04.md
- server/docs/backend-module-boundary.md
- server/docs/persistence-boundary.md
- server/docs/error-response-boundary.md
- server/src/main/java/com/lumix/persistence/package-info.java
- server/src/main/java/com/lumix/persistence/PersistenceRepository.java
- server/src/main/java/com/lumix/persistence/PersistenceErrorCode.java
- server/src/main/java/com/lumix/persistence/PersistenceException.java
- server/src/main/java/com/lumix/api/error/ApiExceptionHandler.java
- server/src/test/java/com/lumix/persistence/PersistenceBoundaryTest.java
Verification run:
- pending
Human review required: no
Next task: none within this requested scope
```
