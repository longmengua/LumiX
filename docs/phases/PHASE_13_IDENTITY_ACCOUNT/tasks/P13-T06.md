# P13-T06 - 建立 API versioning 與 OpenAPI documentation boundary

## 狀態

```text
completed
```

## 目標

建立 API versioning 與 OpenAPI 文件邊界，先把 `/api/v1` 的路徑約定與文件規範固定下來，不實作真正 controller。

## Required context

```text
AGENTS.md
AI_AGENT.md
AI_PROGRESS.md
docs/ai/AI_CONTEXT_ROUTING.md
docs/phases/PHASE_13_IDENTITY_ACCOUNT/README.md
server/docs/backend-module-boundary.md
server/docs/error-response-boundary.md
server/docs/dto-conventions.md
server/docs/api-versioning-openapi-boundary.md
```

## Scope

```text
- 新增 API versioning 文件
- 新增 OpenAPI / Swagger 文件規範
- 新增 API path convention，例如 /api/v1
- 新增 package skeleton 或 marker 類別
- 補測試，驗證 API version path convention 或文件規則
- 更新 backend-module-boundary / error-response-boundary / dto-conventions 文件
```

## Implementation rules

- 不要實作真正 Controller。
- 不要實作 runtime money movement。
- 不要實作 ledger posting。
- 不要實作 reservation hold / release。
- 不要實作 order matching。
- 不要實作 settlement。
- 不要實作 withdrawal signing / broadcast。
- 不要實作 deposit crediting。
- 不要新增或修改 Flyway migration。
- 不要改前端。
- 不要新增 OpenAPI dependency，除非必要且有文件說明原因。
- 所有新增 public class / public method 必須有繁體中文維護性註解。

## Verification

```text
Run the focused API versioning test and the relevant Maven test compile.
```

## Risk

```text
Low。主要風險是未來 controller 若不使用版本化 path convention，會和文件約定脫節。
```

## Completion notes

```text
Implemented by: Codex
Date: 2026-07-07
Files changed:
- docs/phases/PHASE_13_IDENTITY_ACCOUNT/README.md
- docs/phases/PHASE_13_IDENTITY_ACCOUNT/tasks/P13-T06.md
- server/docs/backend-module-boundary.md
- server/docs/error-response-boundary.md
- server/docs/dto-conventions.md
- server/docs/api-versioning-openapi-boundary.md
- server/src/main/java/com/lumix/api/package-info.java
- server/src/main/java/com/lumix/api/version/package-info.java
- server/src/main/java/com/lumix/api/version/ApiVersionedEndpoint.java
- server/src/main/java/com/lumix/api/version/ApiVersionPaths.java
- server/src/main/java/com/lumix/api/version/ApiDocumentationBoundary.java
- server/src/test/java/com/lumix/api/version/ApiVersionBoundaryTest.java
Verification run:
- pending
Human review required: no
Next task: none within this requested scope
```
