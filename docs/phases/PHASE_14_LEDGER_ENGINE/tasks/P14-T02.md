# P14-T02 - Ledger journal draft and invariant contract

## 狀態

```text
completed
```

## 目標

建立 ledger journal draft 的純 domain contract 與 double-entry invariant 檢查，只做 posting 前的 domain 驗證，不接觸任何 runtime ledger mutation。

## Required context

```text
AGENTS.md
AI_AGENT.md
AI_PROGRESS.md
docs/ai/AI_CONTEXT_ROUTING.md
docs/phases/PHASE_14_LEDGER_ENGINE/README.md
docs/phases/PHASE_14_LEDGER_ENGINE/runtime-prerequisites.md
docs/exchange-core/ledger-invariants.md
docs/engineering/code-commenting-standard.md
```

## Scope

```text
- 建立 ledger journal draft 與 entry draft 的純 domain contract
- 建立 double-entry invariant 的純 domain 驗證邏輯
- 建立測試，驗證 journal draft 必須 debit / credit 平衡
- 建立文件，說明 ledger invariant contract
- 建立測試，確認沒有 repository call、@Transactional、posting service、balance mutation
```

## Implementation rules

```text
- 不要新增 LedgerPostingService
- 不要新增 postJournal / reserve / release / commit / rollback 方法
- 不要新增 @Transactional
- 不要呼叫 repository
- 不要 insert ledger_journals / ledger_entries
- 不要更新 balance_projections
- 不要實作 reversal
- 不要實作 settlement
- 不要實作 reservation hold / release
- 不要實作 order matching
- 不要實作 withdrawal signing / broadcast
- 不要新增或修改 Flyway migration
- 不要改前端
- 不要宣稱 production-ready
- 所有 ledger runtime 相關類別與文件都必須標示 HUMAN_REVIEW_REQUIRED
```

## Verification

```text
Run focused ledger domain invariant tests plus a source-scan test that rejects repository / transaction / posting tokens.
```

## Risk

```text
High。ledger invariant 是資金真相前的最後一道 domain gate，若語意混入 runtime 會污染後續 phase。
```

## Completion notes

```text
Implemented by: Codex
Date: 2026-07-08
Files changed:
- docs/phases/PHASE_14_LEDGER_ENGINE/README.md
- docs/phases/PHASE_14_LEDGER_ENGINE/tasks/P14-T02.md
- server/src/main/java/com/lumix/ledger/domain/package-info.java
- server/src/main/java/com/lumix/ledger/domain/LedgerDirection.java
- server/src/main/java/com/lumix/ledger/domain/LedgerBusinessReferenceType.java
- server/src/main/java/com/lumix/ledger/domain/LedgerEntryDraft.java
- server/src/main/java/com/lumix/ledger/domain/LedgerJournalDraft.java
- server/src/main/java/com/lumix/ledger/domain/LedgerInvariantViolation.java
- server/src/main/java/com/lumix/ledger/domain/LedgerInvariantPolicy.java
- server/src/test/java/com/lumix/ledger/domain/LedgerInvariantPolicyTest.java
- server/src/test/java/com/lumix/architecture/P14T02LedgerDomainBoundaryTest.java
Human review required: yes
Verification run:
- `./mvnw -Dtest=LedgerInvariantPolicyTest,P14T02LedgerDomainBoundaryTest test` (from `server/`)
Runtime logic added: no
Migration changed: no
Repository call added: no
Transaction added: no
Skeleton added: yes
Comments completed: yes
Next task: none within this requested scope
```
