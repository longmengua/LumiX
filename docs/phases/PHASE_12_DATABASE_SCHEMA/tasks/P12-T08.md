# P12-T08 - Create outbox, audit log, and idempotency schema

## 狀態

```text
completed
```

## 目標

Create durable side-effect, audit, and duplicate-protection tables.

## Required context

```text
AGENTS.md
AI_AGENT.md
docs/ai/AI_CONTEXT_ROUTING.md
docs/phases/PHASE_12_DATABASE_SCHEMA/README.md
```

## Expected files

```text
outbox_events, idempotency_keys, audit_logs
```

## Implementation rules

- Do not implement runtime money movement.
- 不要跳到後續階段。
- Do not mark TODO / placeholder as production complete.
- 金額值請使用明確精度。
- 新增或更新測試／驗證備註。

## Verification

```text
Schema test verifies unique idempotency scope/key, outbox status indexes, and audit lookup indexes.
```

## Risk

```text
Medium-high. Admin audit semantics need review.
```

## Completion notes

```text
Implemented by: Codex
Date: 2026-07-07
Files changed:
- server/src/main/resources/db/migration/V008__create_outbox_audit_idempotency_tables.sql
- server/src/test/java/com/lumix/database/P12T08SchemaMigrationTest.java
- docs/phases/PHASE_12_DATABASE_SCHEMA/tasks/P12-T08.md
Verification run:
- `./mvnw -Dtest=P12T08SchemaMigrationTest test`
Human review required: yes
Notes:
- V008 only establishes outbox / audit / idempotency data structures; it does not add publisher, writer, or interceptor runtime.
- `request_id` is only for tracking and correlation; it is not a full idempotency guarantee.
- TEXT is used for payload and state snapshots to keep the schema test compatible with H2.
Next task: P12-T09
```
