# P16-T08 - Spot sandbox settlement runtime gate

## Status

```text
completed
```

## Summary

建立 spot sandbox settlement runtime gate，只把 P16-T06 settlement input 轉成 sandbox settlement plan / ledger posting candidate，不執行 ledger posting、balance refresh 或 reservation commit。

## Scope

```text
spot sandbox settlement runtime gate only
no settlement runtime
no DB write
no ledger posting runtime
no balance projection update
no reservation update
```

## Notes

```text
P16-T08 completed as sandbox settlement runtime gate only.
settlement runtime 尚未開始。
```

## Verification

```text
./mvnw -Dtest=P16T08SpotSandboxSettlementRuntimeGateTest,SpotSandboxSettlementRuntimeGateTest test
```
