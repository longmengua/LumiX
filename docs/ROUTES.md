# LumiX 路由索引

## 公開頁面

- `/`：首頁
- `/markets`：市場頁
- `/spot/:symbol`：現貨交易頁（占位）
- `/futures/:symbol`：合約交易頁（占位）
- `/margin/:symbol`：槓桿交易頁（占位）

## 帳戶頁面

- `/assets`：資產頁
- `/orders`：訂單頁
- `/positions`：倉位頁
- `/account`：帳戶總覽

## 管理頁面

- `/admin`：後台總覽（占位）
