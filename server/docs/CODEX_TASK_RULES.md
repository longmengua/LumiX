# CODEX_TASK_RULES

- Codex must update `AI_PROGRESS.md` after each completed phase.
- Codex must not cross phase boundaries.
- `server/` uses Maven + Maven Wrapper as the build tool in Phase 9.
- Do not add Gradle build files when Maven is the active build system.
- Do not retain Gradle wrapper files in `server/`.
- After Phase 9 is done, work must stop and wait for manual review.
- Do not enter Phase 10 before the user reviews and approves Phase 9.
