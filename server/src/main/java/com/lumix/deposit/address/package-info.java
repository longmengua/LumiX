/**
 * P22-T01 入金地址所有權的純 domain contract。
 *
 * <p>此 package 不生成地址、不連鏈、不寫 schema，也不會 credit、修改 balance 或 ledger；它只讓後續觀測流程
 * 能以 canonical network/address identity 判斷地址歸屬。</p>
 */
package com.lumix.deposit.address;
