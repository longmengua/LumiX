/**
 * P22-T04 的唯讀鏈上觀測 reconciliation、evidence 與 P23 handoff 契約。
 *
 * <p>此套件不提供 dashboard/API，不寫入資料庫，也不將候選資料轉成 credit；它只產生可審計、可重播的 pure report。</p>
 */
package com.lumix.deposit.observation.reconciliation;
