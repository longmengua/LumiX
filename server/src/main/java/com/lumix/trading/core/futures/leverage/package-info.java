/**
 * Futures leverage sandbox model。
 *
 * 這個 package 只保存 leverage value object 與 isolated leverage config，不接 margin calculation、
 * margin check、liquidation、funding、matching 或 settlement runtime。
 */
package com.lumix.trading.core.futures.leverage;
