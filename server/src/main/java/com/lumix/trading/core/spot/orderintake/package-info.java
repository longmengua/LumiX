/**
 * Spot Trading Sandbox 的 order intake boundary。
 *
 * 這個 package 只保存 sandbox order command 與 in-memory intake contract，不應被誤解為正式 order placement runtime。
 */
package com.lumix.trading.core.spot.orderintake;
