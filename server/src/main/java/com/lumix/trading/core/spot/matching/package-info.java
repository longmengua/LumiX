/**
 * Spot Trading Sandbox 的 matching design gate。
 *
 * 這個 package 只保存 sandbox matching 的設計契約，不代表正式 matching runtime、trade persistence 或 settlement 已完成。
 */
package com.lumix.trading.core.spot.matching;
