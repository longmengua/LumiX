/**
 * Futures isolated margin position sandbox model。
 *
 * 這個 package 只放 position 身分、方向、數量、entry price 與生命週期模型，
 * 刻意不接 cross margin、margin calculation、liquidation、funding 或 matching runtime。
 */
package com.lumix.trading.core.futures.position;
