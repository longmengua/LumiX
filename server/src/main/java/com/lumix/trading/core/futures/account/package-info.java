/**
 * Futures account sandbox model。
 *
 * 這個 package 只放 futures account 的 immutable domain model，刻意不接 position、leverage、
 * margin calculation、matching 或 settlement runtime。
 */
package com.lumix.trading.core.futures.account;
