/**
 * Trading Runtime Core 的 balance projection rebuild runtime package。
 *
 * 這裡只放保守的 rebuild gate 與 result，避免把 read model 誤接成完整 balance mutation runtime。
 */
package com.lumix.trading.core.projection.runtime;
