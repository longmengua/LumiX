/**
 * Futures sandbox order placement model。
 *
 * 這個 package 只保存 Phase 18-T01 的下單意圖、placement 驗證與 accepted/rejected 結果模型，
 * 刻意不接 order book、matching、fill、trade、position update、reservation、ledger 或 settlement runtime。
 */
package com.lumix.trading.core.futures.order;
