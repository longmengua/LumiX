/**
 * Spot Trading Sandbox 的 in-memory order book boundary。
 *
 * 這個 package 只保存 sandbox order book 與 boundary contract，不應被誤解為正式 order book runtime。
 */
package com.lumix.trading.core.spot.orderbook;
