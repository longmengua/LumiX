/**
 * Futures sandbox 對共用限價單候選配對規則的受限適配層。
 *
 * Phase 18-T02 只輸出 immutable candidate evaluation，不包含 order book、matching execution、fill、trade、position 或結算 runtime。
 */
package com.lumix.trading.core.futures.matching;
