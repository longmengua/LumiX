/**
 * ledger application layer 的入口 package。
 *
 * 這一層只放 ledger runtime 的 application boundary、prerequisite gate 與未來 use case。
 * Phase 14-T01 只建立 skeleton，不放 posting runtime。
 */
package com.lumix.ledger.application;
