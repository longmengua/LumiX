/**
 * Normalized market-data contract 的純 mapping serialization form。
 *
 * <p>此處不提供 JSON、REST、公開串流、broker 或 persistence runtime；wire record 只固定所有文字欄位的
 * 表示方式，使未來獲批准的 transport 可沿用相同契約。</p>
 */
package com.lumix.marketdata.contract.serialization;
